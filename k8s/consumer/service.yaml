apiVersion: v1
kind: Service
metadata:
  name: consumer
  namespace: data-platform
spec:
  type: LoadBalancer
  ports:
    - port: 8081
      targetPort: 8081
      name: http
  selector:
    app: consumer
